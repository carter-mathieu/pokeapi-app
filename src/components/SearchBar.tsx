import React from "react";

const SearchBar = () => {
	return (
		<>
			<form>
				<input type="text" placeholder="Search..." className="input w-full max-w-xs ml-6 mt-2" />
			</form>
		</>
	);
};

export default SearchBar;
